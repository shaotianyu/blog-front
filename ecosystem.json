{
    "apps" : [{
        "name"      : "front",
        "script"    : "server.js",
        "env": {
            "COMMON_VARIABLE": "true"
        },
        "env_production" : {
            "NODE_ENV": "production"
        }
    }],
    "deploy" : {
            "production" : {
                "user" : "root",
                "host" : ["123.207.57.93"],
                "ref"  : "origin/master",
                "repo" : "git@gitee.com:shaotianyu/blog-front.git",
                "path" : "/data/blog/front",
                "post-deploy" : "cnpm install && cnpm run build && pm2 startOrRestart ecosystem.json --env production",
                "ssh_options": "StrictHostKeyChecking=no",
                "env"  : {
                    "NODE_ENV": "production"
            }
        }
    }
}